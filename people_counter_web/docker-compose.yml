version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app.log:/app/app.log
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend 